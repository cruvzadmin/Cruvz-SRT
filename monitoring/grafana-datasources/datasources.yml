apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://monitoring:9090
    isDefault: true
    editable: true

  - name: Loki
    type: loki
    access: proxy
    url: http://log-aggregator:3100
    editable: true